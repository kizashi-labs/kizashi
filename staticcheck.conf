# 公開版（Free）の staticcheck 設定。
#
# U1000（未使用）だけを外す: この版は有償側のハンドラ・テストを同梱しない
# ため、kept 側に残した共有ヘルパの一部が「この木では」未使用になる。
# 呼び手ごと消すと本流との差分が膨らみ、同期のたびに手術が要るので、
# 未使用のまま置くことを選んでいる。それ以外の検査は本流と同じ。
checks = ["inherit", "-U1000"]
