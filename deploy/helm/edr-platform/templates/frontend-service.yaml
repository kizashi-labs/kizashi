apiVersion: v1
kind: Service
metadata:
  name: {{ include "edr-platform.fullname" . }}-frontend
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "edr-platform.frontend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "edr-platform.frontend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.frontend.port }}
      targetPort: http
      protocol: TCP
