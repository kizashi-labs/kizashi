// Package agentcontract holds contract tests that measure the whole agent
// module rather than one package.
//
// **本番のコードは1行もありません。** ここにあるのは、`agent/` 全体を
// ソースとして読み、規則に合わない箇所を数える検査です。特定のパッケージに
// 置くと、そのパッケージの話に見えてしまいます。
package agentcontract
