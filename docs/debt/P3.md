# 🔵 P3 — 機能カバレッジ（準備中ページ）

> 台帳の索引は [../技術的負債と改善計画.md](../技術的負債と改善計画.md)。

### P3-1. BackendPendingBanner 掲示ページ 61件
- 定義: `frontend/components/layout/BackendPendingBanner.tsx:10-72`
- 全397ページ中 63ページ（約16%）が準備中。`/admin/` 配下に 45件集中。
- **方針:** 量より顧客需要の高い順に。候補: `custom-alert-rules` / `geo-blocking` /
  `quarantine` / `ip-blocklist` / `saved-searches`。

### P3-2. テーブル非存在で 503 を返す箇所 92件
- パターン: `pg_tables` 存在確認 → なければ 503。
- 多くは未適用 migration が原因。実装時にあわせて migration を整備する。
