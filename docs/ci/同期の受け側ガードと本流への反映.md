# 同期の受け側ガードと、本流へ反映すること

> **読む人**: 本流（生成器）側を触れる人。
> **要点**: 受け側のガードは「消されたことに気づく」までしかできません。
> **消されなくする**には、下の 3 件を生成器側に入れる必要があります。

---

## 1. 何が起きたか

このリポジトリは本流のスナップショットで、生成のたびに木がまるごと
差し替わります。**生成側が何かを落とすと、その PR は緑のまま入ります。**

実例が 2 度あります。

| PR | 何が起きたか |
| --- | --- |
| #67 | ジョブの `timeout-minutes` と、**欠落を落とす検査**を入れた |
| #70 | 同期がその全部を消し、**同時に検査そのものも消した** |
| #73 | 戻した |

#70 のとき「`timeout-minutes` がありません」と怒る側はもういなかったので、
PR は 22/22 緑で通りました。**検査が自分と一緒に消えるので、検査は自分を
守れません。**

`98107b9 → 21df049`（#67 → #70）の差分がそのまま「何が落ちるか」です:

```
 .github/workflows/agent-ebpf.yml              |  2 -
 .github/workflows/agent-os-tests.yml          |  2 -
 .github/workflows/attack-scorecard.yml        |  1 -
 .github/workflows/ci.yml                      | 21 -----
 .github/workflows/integration.yml             |  2 -
 .github/workflows/merge-gate.yml              |  2 -
 .github/workflows/package.yml                 |  6 ---
 .github/workflows/security.yml                |  6 ---
 .github/workflows/verify-prevention-build.yml |  3 --
 .github/workflows/workflow-lint.yml           | 28 -----   ← 検査自身
 scripts/verify.sh                             | 74 -----   ← ローカルの写し
 11 files changed, 3 insertions(+), 144 deletions(-)
```

**選択的な削除ではありません。** 全部消え、同時に欠落検査そのものも
消えます。

## 2. いま守られていること —— と、守れないこと

#76 で受け側のガードを入れました（`scripts/sync_guard.py` と
`.github/workflows/sync-guard.yml`）。`pull_request_target` で走るので、
**PR が定義を消しても main の版が走ります。**

守っているもの:

| 対象 | 留め方 |
| --- | --- |
| ジョブの上限 | 欠落と 60 分超を落とす |
| step の上限 | **数**で留める（名前ではない。本流は step を改名するので、名前で留めると改名のたびに鳴る。**鳴る検査は消されます**） |
| `workflow-lint.yml` | ファイルの有無ではなく、**中の判定 2 本**を文字列で |
| `scripts/verify.sh` | 同じ 3 本 |
| ラチェット 16 本 | 一覧で |
| 較正の道具 3 本 | 一覧で |
| ガード自身 3 本 | 一覧で（**消えても、その回は止まりません** —— base 側で走るので。止まるのは次の PR で、そのときにはもう誰も見ていません。だから「消えた」ことが消えた回の画面に出る必要があります） |
| `agent-ebpf` の `mkdir` | 再試行ループの**内側**にあること |

**守れないこと** —— ガードは受け取った木しか見えません:

- 生成器が較正の道具を**呼んでいるか**は見えません。ファイルが残って
  いても、呼ばれなければラチェットは次の同期でまた外れます。
- 1 本ずつの改名や追加は捕まえません。捕まえるのは #70 で実際に起きた形
  （**まとめて全部消える**）です。

## 3. 本流でやること（3 件）

### 3-1. `timeout-minutes` 49 件と、欠落検査と、ローカルの写し

生成器側で 49 件を残すか、`workflow-lint.yml` を**再生成の対象から外す**か、
どちらかを入れないと 3 度目が起きます。

現物は §4 の表です。数字は実測（直近 200 runs）の成功時最大に余裕を
足したもので、**遅い日を殺さず、ハングは早く切る**幅にしてあります。

欠落検査の本体:

- `.github/workflows/workflow-lint.yml`
  - `timeout-minutes` が無いジョブ → 落とす（既定が **6 時間**である
    説明つき）
  - `timeout-minutes > 60` → 落とす（「**ハングを上限の引き上げで
    直さないでください**」）
- `scripts/verify.sh` — 同じ判定の写しを、**領域に関係なく毎回**走らせる節

`workflow-lint.yml` を `ci.yml` の中に置いていないのは意図です。
**ci.yml が起動しないなら検査も動かない**ので、別ファイルにしてあります。

なぜ上限が要るのか（実測）:

| ジョブ | 中央 | 最悪 |
| --- | --- | --- |
| `agent-build (linux/amd64/ebpf)` | 1.7 分 | **360.3 分**（＝既定 6 時間の上限に当たって停止） |
| `rules-validate` | 1.2 分 | **111.2 分** |

どちらも `apt-get` の取得で止まっていました。**落ちないので誰も気づかず、
予算だけが減ります。**

### 3-2. `agent-ebpf.yml` の `mkdir -p downloads` の位置

再試行ループの**内側**に置いてください。`downloads/` は `.gitignore` に
入っているのでクローン直後には存在せず、**再試行の間に挟まる
`git reset --hard` が消します。**

この 1 行で、このワークフローはこのリポジトリで初めて成功しました
（それまで 4/4 失敗、すべて同じ位置）。

**この step は PR では走りません**（`if: github.ref == 'refs/heads/main'`）。
つまり**消えても PR は緑です。**

### 3-3. 生成器から較正の道具を呼ぶ

```
scripts/recalibrate_ratchets.py --apply
scripts/reanchor_mutations.py --apply
```

**順番が要ります。** 固定値を下げたあと、その値を参照している変異仕様書の
pattern も一緒に動かさないと、**下げた瞬間に仕様書が死にます。**

これを入れないと、ラチェット 16 本は「生成のたびに約 50 個の固定値を
測り直す負担」として、また配布から外されます（#70 がそうしました）。
道具は #75 で作ってあります —— **呼ばれていないだけです。**

## 4. `timeout-minutes` の現物（job 31 + step 18 ＝ 49 件）

> 以前の引き継ぎに「47 件」と書いたものがあります。**古い数です** ——
> `sync-guard.yml`（#76）が job 1 + step 1 を足したので 49 になりました。
> 数は増えるので、**表を写す前に §5 で数え直してください。**

### ジョブ 31 件

| ファイル | ジョブ | 分 |
| --- | --- | --- |
| agent-ebpf.yml | build-ebpf | 20 |
| agent-os-tests.yml | windows-tests | 20 |
| agent-os-tests.yml | macos-tests | 20 |
| attack-scorecard.yml | scorecard | 15 |
| ci.yml | changes | 5 |
| ci.yml | server-test | 30 |
| ci.yml | rules-validate | 15 |
| ci.yml | agent-build | 20 |
| ci.yml | agent-test | 45 |
| ci.yml | server-vulncheck | 15 |
| ci.yml | agent-vulncheck | 15 |
| ci.yml | frontend-build | 40 |
| ci.yml | sdk-test | 15 |
| ci.yml | scripts-test | 20 |
| ci.yml | backup-test | 15 |
| integration.yml | e2e | 30 |
| integration.yml | load-test | 20 |
| merge-gate.yml | consistency | 10 |
| merge-gate.yml | radar | 10 |
| package.yml | linux-packages | 30 |
| package.yml | windows-msi | 30 |
| package.yml | macos-pkg | 30 |
| package.yml | attach | 15 |
| security.yml | trivy-fs | 15 |
| security.yml | trivy-image | 25 |
| security.yml | semgrep | 20 |
| security.yml | gitleaks | 10 |
| sync-guard.yml | distribution | 10 |
| verify-prevention-build.yml | linux-prevention | 25 |
| verify-prevention-build.yml | macos-esf | 20 |
| workflow-lint.yml | lint | 10 |

### step 18 件（すべて 10 分）

ネットワーク取得を踏む step に付けています。**ジョブの上限だけでは
遅いです** —— apt が止まったジョブは job の上限（20〜45 分）まで待って
から落ちます。step 側で切ると 10 分で落ちます。

| ファイル | ジョブ | step |
| --- | --- | --- |
| agent-ebpf.yml | build-ebpf | Install eBPF toolchain (clang + bpftool from source) |
| ci.yml | server-test | staticcheck |
| ci.yml | rules-validate | Install yara CLI |
| ci.yml | agent-build | Generate report-only eBPF bindings (Linux only) |
| ci.yml | agent-test | Generate eBPF bindings (report-only + prevention LSM) |
| ci.yml | agent-test | staticcheck |
| ci.yml | server-vulncheck | Set up Go |
| ci.yml | server-vulncheck | Install govulncheck |
| ci.yml | agent-vulncheck | Install govulncheck |
| ci.yml | sdk-test | Install Python SDK dev dependencies |
| ci.yml | scripts-test | Install PyYAML |
| integration.yml | load-test | Install k6 |
| package.yml | linux-packages | Install nfpm |
| package.yml | linux-packages | Sign packages |
| security.yml | gitleaks | actions/checkout |
| security.yml | gitleaks | Run gitleaks |
| sync-guard.yml | distribution | Install PyYAML |
| verify-prevention-build.yml | linux-prevention | Generate eBPF bindings (report-only + prevention LSM) |

> `sync-guard.yml` の tarball を取る step には step 上限を付けていません。
> **ジョブが 10 分なので、step に 10 分を書いても同じところで切れます。**
> 付けるとしたら、それは規則を揃えるためであって、時間を縮めるためでは
> ありません。

## 5. 数え直し方

```sh
cd .github/workflows
python3 - <<'PY'
import glob, yaml
job = step = 0
for f in sorted(glob.glob('*.yml')):
    for jn, j in (yaml.safe_load(open(f)).get('jobs') or {}).items():
        t = j.get('timeout-minutes')
        print(f"{f}\t{jn}\t{t if t is not None else 'MISSING'}")
        job += t is not None
        for s in (j.get('steps') or []):
            if 'timeout-minutes' in s:
                step += 1
                print(f"    ↳ {s.get('name') or s.get('uses')}\t{s['timeout-minutes']}")
print(f"job={job} step={step} total={job+step}")
PY
```

`MISSING` が 1 つでも出たら、`workflow-lint` と `sync-guard` の両方が
落ちます。

## 6. 関連

- `scripts/sync_guard.py` — 受け側のガード本体。**何を守っているかは
  ここのコメントが正**です
- `scripts/sync_guard_test.py` — ガード自身の判定。**確かめる道具が
  壊れていることが、いちばん高くつきます** —— 以後どの同期も
  「配布物はすべて残っています」と報告し、それは正しく見えます
- `.github/workflows/sync-guard.yml` — なぜ `pull_request_target` なのか、
  なぜ PR の木を展開しないのかの説明
- `scripts/verify.sh` — CI と 1:1 のローカル実行

> **`Sync Guard / The sync did not drop anything` は required status check に
> してください。** required でないと、同期 PR は「赤いが入れられる」状態に
> なります —— #70 は緑で入りましたが、次は赤で入ります。設定には admin
> 権限が要ります。
