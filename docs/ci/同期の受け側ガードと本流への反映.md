# 同期の受け側ガードと、本流へ反映すること

> **読む人**: 受け側（このリポジトリ）のガードを触る人。
> **要点**: 受け側のガードは「消されたことに気づく」までしかできません。
>
> **本流へ渡すだけなら、この文書は要りません** ——
> [本流へ渡す作業一覧.md](本流へ渡す作業一覧.md) の 1 枚で足ります。

---

## 1. 何が起きたか

このリポジトリは本流のスナップショットで、生成のたびに木がまるごと
差し替わります。**生成側が何かを落とすと、その PR は緑のまま入ります。**

実例が **3 度**あります。

| PR | 何が起きたか |
| --- | --- |
| #67 | ジョブの `timeout-minutes` と、**欠落を落とす検査**を入れた |
| #70 | 同期がその全部を消し、**同時に検査そのものも消した** |
| #73 | 戻した |
| #74 | **また全部消えた。** 今度はガードが数えました（下記） |

#70 のとき「`timeout-minutes` がありません」と怒る側はもういなかったので、
PR は 22/22 緑で通りました。**検査が自分と一緒に消えるので、検査は自分を
守れません。**

`98107b9 → 21df049`（#67 → #70）の差分がそのまま「何が落ちるか」です:

```
 .github/workflows/agent-ebpf.yml              |  2 -
 .github/workflows/agent-os-tests.yml          |  2 -
 .github/workflows/attack-scorecard.yml        |  1 -
 .github/workflows/ci.yml                      | 21 -----
 .github/workflows/integration.yml             |  2 -
 .github/workflows/merge-gate.yml              |  2 -
 .github/workflows/package.yml                 |  6 ---
 .github/workflows/security.yml                |  6 ---
 .github/workflows/verify-prevention-build.yml |  3 --
 .github/workflows/workflow-lint.yml           | 28 -----   ← 検査自身
 scripts/verify.sh                             | 74 -----   ← ローカルの写し
 11 files changed, 3 insertions(+), 144 deletions(-)
```

**選択的な削除ではありません。** 全部消え、同時に欠落検査そのものも
消えます。

### #74 —— 3 度目。ガードが数えた 64 件

`sync/main-20260820` にガードを当てた結果です（`conclusion: failure`）。

| 節 | 件数 |
| --- | --- |
| ジョブの上限 | 29 |
| 取得する step の上限 | 7 |
| 欠落を落とす検査そのもの | 2 |
| ローカルの写し | 3 |
| ラチェット 16 本 | 16 |
| **この検査そのもの** | **3** |
| 較正の道具 | 3 |
| `agent-ebpf` の `downloads/` | 1 |
| **合計** | **64** |

`sync_guard.py` / `sync_guard_test.py` / `sync-guard.yml` の 3 本とも
消えています。**ガードは自分が消されることを、消される回に報告しました**
—— #70 のときに出来なかったことです。

**この PR はチェック 0 件のまま開いていました**（§2 の「遡って走らない」）。
閉じて開き直して初めて走りました。

## 2. いま守られていること —— と、守れないこと

#76 で受け側のガードを入れました（`scripts/sync_guard.py` と
`.github/workflows/sync-guard.yml`）。`pull_request_target` で走るので、
**PR が定義を消しても main の版が走ります。**

守っているもの:

| 対象 | 留め方 |
| --- | --- |
| ジョブの上限 | 欠落と 60 分超を落とす |
| step の上限 | **数**で留める（名前ではない。本流は step を改名するので、名前で留めると改名のたびに鳴る。**鳴る検査は消されます**） |
| `workflow-lint.yml` | ファイルの有無ではなく、**中の判定 2 本**を文字列で |
| `scripts/verify.sh` | 同じ 3 本 |
| ラチェット 16 本 | 一覧で |
| 較正の道具 3 本 | 一覧で |
| ガード自身 3 本 | 一覧で（**消えても、その回は止まりません** —— base 側で走るので。止まるのは次の PR で、そのときにはもう誰も見ていません。だから「消えた」ことが消えた回の画面に出る必要があります） |
| `agent-ebpf` の `mkdir` | 再試行ループの**内側**にあること |
| ラチェットの固定値 | **緩んだ向きだけ**を落とす（下記） |

### ラチェットの固定値 —— 「呼ばれたか」は見えないが、結果は見える

ここには以前「生成器が較正の道具を**呼んでいるか**は見えないので守れない」
と書いてありました。**呼んだかどうかは確かに見えませんが、呼ばれなかった
結果は見えます** —— 固定値が main と違う値で戻ってきます。

判定を実際に読んで 3 種類に分けました。守るのは 2 つだけです。

| 種類 | 判定の形 | 緩む向き | 守るか |
| --- | --- | --- | --- |
| 上限 | `if got > N` | **N が増える** | 落とす |
| 下限 | `if got < N` | **N が減る** | 落とす |
| 実数一致 | `if got != N` | **無い** | 見ない |

**実数一致を見ないのは手抜きではありません。** ずれた瞬間にその検査自身が
落ちるので、静かに退行できません。ここで鳴らすと、正当な更新のたびに
赤くなります —— **鳴る検査は消されます。**

現物は上限 14・下限 7・実数一致 14（＋ラチェットではない局所変数 2）で、
`sync_guard.py` の `RATCHET_LIMITS` に名前で並べてあります。
**表に無い固定値が出てきたら「分類してください」で落とします。** 既定を
「見逃す」にすると、新しいラチェットは黙って守られないままになります。

> **向きを名前から推測しないでください。** `min*` や `*_CEILING` は
> だいたい合っていますが、`floor = 2000` や `NAV_PENDING` のように
> 名前では決まらないものがあります。取り違えると、**捕まえるべき退行
> だけを黙って通します。**
> `sync_guard_test.py` が、実物にある固定値がすべて分類されていること
> と、表に実物に無い名前が残っていないことを**両方向**で留めます。

**守れないこと** —— ガードは受け取った木しか見えません:

- 較正の道具が**呼ばれたかどうか**そのものは見えません。上の検査が
  見るのは結果（値が緩んだか）だけなので、**値が動かない回は素通り
  します。** 呼ぶこと自体は生成器側にしか入れられません
  （[本流へ渡す作業一覧.md](本流へ渡す作業一覧.md) の §1-3）。
- 1 本ずつの改名や追加は捕まえません。捕まえるのは #70 で実際に起きた形
  （**まとめて全部消える**）です。
- **ガードより前に開かれた PR には、遡って走りません。**（下記）

### ガードは、自分より前に開かれた PR を見ていません

`pull_request_target` が走るのは PR の**イベント**（open / synchronize /
reopen）です。ガードが main に入る前に開かれ、その後 push されていない
PR には、**一度も走りません。**

実例がいま開いています。**#74（同期 PR、削除 21,088 行）は
`total_count: 0` —— チェックが 1 件もありません。** 06:48Z に開かれ、
ガードは #76 でそのあとに入りました。

つまり **`sync/main-20260820` は、ガードが見たことのない木です。**
required status check を入れても、**チェックが 0 件の PR は「必須の
チェックが完了していない」として止まる**ので入りませんが、止まっている
理由が「ガードが落ちた」ではなく「ガードが走っていない」であることを
読み違えないでください。

走らせるには、**その PR を閉じて開き直してください**（`reopened` で走ります）。
ブランチには何も push されません。空コミットを push するのでも走りますが、
同期ブランチの履歴に生成器が作っていないコミットが混ざります。

> **`workflow_dispatch` では走りません。** ここには以前「`workflow_dispatch`
> で回してください」と書いてありました。**間違いでした。**
> dispatch では `github.event.pull_request.head.sha` が空になり、
> **ref の無い tarball は既定ブランチを返します** —— ガードは main を見て
> 「配布物はすべて残っています」と**緑を返します。**
>
> **落ちるより悪い形です。** 手で回した人は確かめたつもりで、PR の木を
> 一度も見ていません。空 SHA を弾く枝を足して塞ぎました（#89 / #90）ので、
> いまは緑ではなく落ちます。

### ガード自身の `run:` は、1 つの PR では変えられません

`sync-guard.yml` の `run:` を書き換えると、**その PR は落ちます。**
この検査は base（main）の版で走るので、main の
`GUARD_ALLOWED_RUNS` に無い形は「許していない run:」になります。

**それは正しい挙動です。** この workflow は base の権限で走るので、
`run:` の書き換えが黙って入るほうが危ない。

変えるときは 2 つの PR に分けてください:

| | やること |
| --- | --- |
| 1 | 新しい形を**先に** `GUARD_ALLOWED_RUNS` へ足す。**workflow は触らない** |
| 2 | `sync-guard.yml` を新しい形にし、**古い形を一覧から消す** |

**2 で古い形を消し忘れないでください。** 両方許したままだと、新しい形が
持っている検査（いまなら空 SHA を弾く枝）を消しても落ちません ——
**塞いだはずの穴が、静かに開いたままになります。**
`sync_guard_test.py` の `test_dropping_the_empty_sha_branch_fires` が
そこを留めます（古い形を一覧に戻すと、この検査が落ちます）。

実例は #89（1 段目）と #90（2 段目）です。

## 3. 本流でやること —— 別の文書に移しました

やること 3 件と、`timeout-minutes` の現物の表と、その数え直し方は
**[本流へ渡す作業一覧.md](本流へ渡す作業一覧.md)** にあります。

**写しを置いていないのは意図です。** 同じ表を 2 か所に置くと、片方だけが
更新されます —— この表は一度それで腐りました（「47 件」のまま残り、
実物は 49 件になっていました）。**渡すのはあちらの 1 枚だけです。**

この文書に残っているのは**受け側の事情**（何が起きたか、ガードが何を
守っていて何を守れないか）で、本流を触る人には要りません。

---

## 4. required status check の設定 —— 効かない形が 3 つあります

**`The sync did not drop anything` は required status check にしてあります**
（Settings → Rules → Rulesets、`Require status checks` という名前の ruleset）。
required でないと、同期 PR は「赤いが入れられる」状態になります ——
#70 は緑で入りましたが、次は赤で入ります。設定には admin 権限が要ります。

外したり作り直したりするときのために、**設定したのに効かなかった形を
3 つとも書いておきます。** どれも画面上は「設定してある」ように見えます。

| 落とし穴 | 画面の見え方 | 実際 |
| --- | --- | --- |
| 名前が表示形 | チェックが一覧に載っている | **一致する check が無く、永久に待ち続けます** |
| Enforcement が `Disabled` | ルールの中身は全部入っている | **評価されません** |
| Target branches が空 | 同上 | **どのブランチにも掛かりません** |

**設定欄に入れる名前は `The sync did not drop anything` です。**
`Sync Guard` はワークフロー名で、status check の名前ではありません
（画面には `Sync Guard / The sync did not drop anything` と出ますが、
それは表示形です）。ここには一度その表示形を書いていて、**そのせいで
設定が効きませんでした。**

`Enforcement status` には `Evaluate` もあります。**あれは掛かるはずだった
かを記録するだけで、マージは止めません。** 止めたいなら `Active` です。

`Target branches` が空のとき、GitHub は
`This ruleset does not target any resources and will not be applied.`
と黄色で出します。**中のルールをいくら正しく書いても、一切適用されません。**

### 効いているかの確かめ方

PR の `mergeable_state` を見てください。

| 値 | 意味 |
| --- | --- |
| `blocked` | **required なチェックが待ち／落ちている** —— 効いています |
| `unstable` | 落ちている／待っているチェックはあるが、**required は 1 つも無い** |
| `dirty` | 衝突。**上の 2 つを覆い隠します** |
| `clean` | 入れられる |

**`unstable` は「required が無い」ことの証拠です。** 設定したつもりで
`unstable` が出たら、上の表の 3 つを疑ってください。

**draft かどうかは関係ありません。** ここで実測しています（#93、2026-08-21）:

| 状態 | `mergeable_state` |
| --- | --- |
| draft・チェック 4 件が実行中 | `blocked` |
| draft・4 件とも緑 | `clean` |

draft のまま緑にしたら `clean` になったので、**`blocked` を出していたのは
draft ではなく、待っていた required なチェックです。** 設定前の同じ場面は
`unstable` でした。

ただしこれで分かるのは「required なチェックが 1 つ以上あって、それが
待っていた」ところまでです。**`The sync did not drop anything` という
名前そのものを確かめるには、ガードが落ちる PR が要ります。**

### ガードは全 PR に走ります

`sync-guard.yml` は `pull_request_target: branches: [main]` です ——
**同期 PR だけでなく、main 宛ての PR すべてで走ります。** なので required に
しても「同期以外の PR が、報告されないチェックを永久に待つ」形にはなりません。

ただし **§2 の「遡って走りません」はここにも効きます。** ガードが main に
入る前に開かれ、その後 push されていない PR は、required にすると
**チェック 0 件のまま止まり続けます。** 閉じて開き直してください。

---

## 5. 関連

- `scripts/sync_guard.py` — 受け側のガード本体。**何を守っているかは
  ここのコメントが正**です
- `scripts/sync_guard_test.py` — ガード自身の判定。**確かめる道具が
  壊れていることが、いちばん高くつきます** —— 以後どの同期も
  「配布物はすべて残っています」と報告し、それは正しく見えます
- `.github/workflows/sync-guard.yml` — なぜ `pull_request_target` なのか、
  なぜ PR の木を展開しないのかの説明
- `scripts/verify.sh` — CI と 1:1 のローカル実行
