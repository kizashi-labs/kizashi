# 本流へ渡す作業一覧

> **読む人**: 本流（生成器）側を触れる人。
> **これだけで足ります。** 受け側の事情を知らなくても、下の 3 件を
> 入れれば済むように書いてあります。

このリポジトリは本流のスナップショットで、生成のたびに木がまるごと
差し替わります。**生成側が何かを落とすと、その PR は緑のまま入ります。**

実際に **3 度**起きました（#67 で入れ、#70 で消え、#73 で戻し、#74 でまた
消えた）。#74 では受け側のガードが **64 件**を数えました —— そこには
**ガード自身の 3 ファイル**も含まれています。

受け側のガードは「**消されたことに気づく**」までしかできません。
**消されなくする**には、下の 3 件が生成器側に要ります。

経緯と受け側の作りは
[同期の受け側ガードと本流への反映.md](同期の受け側ガードと本流への反映.md)
にあります。**渡すだけならこの文書で足ります。**

---

## 1. やること（3 件）

> **どこに入れるか。** 生成器（`scripts/public-snapshot/`）には、使える段が
> **すでに 2 つ**あります。どちらも、配られたこの木に痕跡が残っています ——
> 受け側から見えるのはこの 2 か所だけなので、名前はそこから採りました。
>
> | 段 | 何をしているか | この木に残っている証拠 |
> | --- | --- | --- |
> | `overlay` | 公開版向けに**ファイルごと差し替える** | `server/migrations/` の 4 本。冒頭に「公開版向けの差し替え（`scripts/public-snapshot/overlay`）」と書いてあります |
> | `adjust-mutations.py` | 木を配った**あとに書き換える** | `scripts/mutations/NOT_SHIPPED.txt` の 1 行目が名指ししています |
>
> ```
> # この配置が同梱しないファイル。scripts/public-snapshot/adjust-mutations.py が
> # 生成する。手で編集しない（次の生成で上書きされる）。
> ```
>
> **下の 3 件は、この 2 つのどちらかに入ります** —— §1-1 は好きなほうで、
> §1-2 は `overlay`（ワークフローの中身の話なので）、§1-3 は「あと」の段です。
> 新しい仕組みは要りません。
>
> **そして §1-1 と §1-3 は、1 本にまとめてあります。** 配ったあとの段に、
> この 1 行を足してください:
>
> ```sh
> python3 scripts/after_snapshot.py --apply
> ```
>
> 中で 3 つを順番に呼びます（`--restore` → `recalibrate` → `reanchor`）。
> **順番は散文ではなくコードにしてあります** —— 散文の順番は、
> 読み飛ばされても何も落ちません。`--apply` を付けずに呼ぶと、
> 3 つとも「見るだけ」で走るので、入れる前に確かめられます。

### 1-1. `timeout-minutes` 49 件と、欠落検査と、ローカルの写し

入れ方は 3 つあります。**どれも入れないと 4 度目が起きます**
（3 度起きています —— #67 → #70 → #73 → #74）。

1. 生成器側で 49 件を残す
2. `.github/workflows/` を `overlay` に置き、**再生成の対象から外す** ——
   消える経路そのものが無くなります。migrations の 4 本と同じやり方です
3. 生成のあとに**下の道具を呼ぶ**（`adjust-mutations.py` と同じ段）

**2 と 3 は排他ではありません。** 2 は「消させない」、3 は「消えても戻す」で、
効く場面が違います —— `overlay` に入っていないファイルに上限が増えたときは
3 だけが拾います。

現物は §2 の表です。数字は実測（直近 200 runs）の成功時最大に余裕を
足したもので、**遅い日を殺さず、ハングは早く切る**幅にしてあります。

**表を書き写す必要はありません。** §2 の表は手で書いた散文ではなく、
`scripts/handover_timeouts.py --apply` の出力で、同じ道具が逆向きにも
走ります:

```sh
# 配った木に対して、生成のあとに 1 回
# （`scripts/public-snapshot/adjust-mutations.py` と同じ段）
python3 scripts/handover_timeouts.py --restore .

# §1-3 と一緒に呼ぶなら、こちら 1 行で足ります
python3 scripts/after_snapshot.py --apply
```

持っていくのは 2 つです —— **道具**（`scripts/handover_timeouts.py`）と
**値**（この文書）。木に無い上限だけを、`runs-on:` の次の行と、
名前で見つけた step の中に入れます。

- **冪等です。** すでに入っている値は触りません。
- **違う値が入っていたら上書きしません** —— どちらが正しいかは、
  道具には決められません。名指しで報告して 1 を返します。
- **改名された step は、黙って飛ばしません。** 本流は step を改名するので、
  見つからない行が出るのは想定内です。**想定内だから黙る**のがいちばん
  危ない —— 飛ばすと、その step の上限だけが消えます。

これで生成器側の作業は「49 か所に書き写す」ではなく「**1 つ呼ぶ**」に
なります。§1-3 の較正の道具と同じ形です。

欠落検査の本体:

- `.github/workflows/workflow-lint.yml`
  - `timeout-minutes` が無いジョブ → 落とす（既定が **6 時間**である
    説明つき）
  - `timeout-minutes > 60` → 落とす（「**ハングを上限の引き上げで
    直さないでください**」）
- `scripts/verify.sh` — 同じ判定の写しを、**領域に関係なく毎回**走らせる節

`workflow-lint.yml` を `ci.yml` の中に置いていないのは意図です。
**ci.yml が起動しないなら検査も動かない**ので、別ファイルにしてあります。

なぜ上限が要るのか（実測）:

| ジョブ | 中央 | 最悪 |
| --- | --- | --- |
| `agent-build (linux/amd64/ebpf)` | 1.7 分 | **360.3 分**（＝既定 6 時間の上限に当たって停止） |
| `rules-validate` | 1.2 分 | **111.2 分** |

どちらも `apt-get` の取得で止まっていました。**落ちないので誰も気づかず、
予算だけが減ります。**

### 1-2. `agent-ebpf.yml` の `mkdir -p downloads` の位置

再試行ループの**内側**に置いてください（`overlay` に置くなら、それで
そのまま済みます）。`downloads/` は `.gitignore` に
入っているのでクローン直後には存在せず、**再試行の間に挟まる
`git reset --hard` が消します。**

この 1 行で、このワークフローはこのリポジトリで初めて成功しました
（それまで 4/4 失敗、すべて同じ位置）。

**この step は PR では走りません**（`if: github.ref == 'refs/heads/main'`）。
つまり**消えても PR は緑です。**

### 1-3. 生成器から較正の道具を呼ぶ

```sh
# ここも `scripts/public-snapshot/` の、木を配ったあとの段です
python3 scripts/recalibrate_ratchets.py --apply
python3 scripts/reanchor_mutations.py --apply

# §1-1 と一緒に、順番ごと固定して呼ぶなら
python3 scripts/after_snapshot.py --apply
```

**順番が要ります。** 固定値を下げたあと、その値を参照している変異仕様書の
pattern も一緒に動かさないと、**下げた瞬間に仕様書が死にます。**

**逆に呼んでも、3 本とも 0 を返します。** 出力では見分けが付かず、
壊れたことが分かるのは次の同期で、そのとき変異仕様書は全 SKIP で緑を
返しています。だから `after_snapshot.py` は、順番と「2 が落ちたら 3 を
走らせない」ところまでコードにしてあります（`after_snapshot_test.py`
がそこを留めます）。

これを入れないと、ラチェット 16 本は「生成のたびに約 50 個の固定値を
測り直す負担」として、また配布から外されます（#70 がそうしました）。
道具は #75 で作ってあります —— **呼ばれていないだけです。**

受け側は**緩んだ結果**を落とすようにしましたが、それは事後です。
値が動かない回は素通りしますし、緩んだと分かっても受け側では直せません
（正しい値は生成器側でしか測れません）。**呼ぶこと自体は、ここでしか
入れられません。**

## 2. `timeout-minutes` の現物（job 31 + step 18 ＝ 49 件）

> 以前の引き継ぎに「47 件」と書いたものがあります。**古い数です** ——
> `sync-guard.yml`（#76）が job 1 + step 1 を足したので 49 になりました。
> **数は増えます。** この表は `--apply` で生成し、受け側の CI が行ごとに
> 実物と突き合わせています（§3）。写すのではなく `--restore` で
> 入れてください。

### ジョブ 31 件

| ファイル | ジョブ | 分 |
| --- | --- | --- |
| agent-ebpf.yml | build-ebpf | 20 |
| agent-os-tests.yml | windows-tests | 20 |
| agent-os-tests.yml | macos-tests | 20 |
| attack-scorecard.yml | scorecard | 15 |
| ci.yml | changes | 5 |
| ci.yml | server-test | 30 |
| ci.yml | rules-validate | 15 |
| ci.yml | agent-build | 20 |
| ci.yml | agent-test | 45 |
| ci.yml | server-vulncheck | 15 |
| ci.yml | agent-vulncheck | 15 |
| ci.yml | frontend-build | 40 |
| ci.yml | sdk-test | 15 |
| ci.yml | scripts-test | 20 |
| ci.yml | backup-test | 15 |
| integration.yml | e2e | 30 |
| integration.yml | load-test | 20 |
| merge-gate.yml | consistency | 10 |
| merge-gate.yml | radar | 10 |
| package.yml | linux-packages | 30 |
| package.yml | windows-msi | 30 |
| package.yml | macos-pkg | 30 |
| package.yml | attach | 15 |
| security.yml | trivy-fs | 15 |
| security.yml | trivy-image | 25 |
| security.yml | semgrep | 20 |
| security.yml | gitleaks | 10 |
| sync-guard.yml | distribution | 10 |
| verify-prevention-build.yml | linux-prevention | 25 |
| verify-prevention-build.yml | macos-esf | 20 |
| workflow-lint.yml | lint | 10 |

### step 18 件

ネットワーク取得を踏む step に付けています。**ジョブの上限だけでは
遅いです** —— apt が止まったジョブは job の上限（20〜45 分）まで待って
から落ちます。step 側で切ると 10 分で落ちます。

| ファイル | ジョブ | step | 分 |
| --- | --- | --- | --- |
| agent-ebpf.yml | build-ebpf | Install eBPF toolchain (clang + bpftool from source) | 10 |
| ci.yml | server-test | staticcheck | 10 |
| ci.yml | rules-validate | Install yara CLI | 10 |
| ci.yml | agent-build | Generate report-only eBPF bindings (Linux only) | 10 |
| ci.yml | agent-test | Generate eBPF bindings (report-only + prevention LSM) | 10 |
| ci.yml | agent-test | staticcheck | 10 |
| ci.yml | server-vulncheck | Set up Go | 10 |
| ci.yml | server-vulncheck | Install govulncheck | 10 |
| ci.yml | agent-vulncheck | Install govulncheck | 10 |
| ci.yml | sdk-test | Install Python SDK dev dependencies | 10 |
| ci.yml | scripts-test | Install PyYAML | 10 |
| integration.yml | load-test | Install k6 | 10 |
| package.yml | linux-packages | Install nfpm | 10 |
| package.yml | linux-packages | Sign packages | 10 |
| security.yml | gitleaks | uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 | 10 |
| security.yml | gitleaks | Run gitleaks (repo .gitleaks.toml allowlist applied) | 10 |
| sync-guard.yml | distribution | Install PyYAML | 10 |
| verify-prevention-build.yml | linux-prevention | Generate eBPF bindings (report-only + prevention LSM) | 10 |

> `sync-guard.yml` の tarball を取る step には step 上限を付けていません。
> **ジョブが 10 分なので、step に 10 分を書いても同じところで切れます。**
> 付けるとしたら、それは規則を揃えるためであって、時間を縮めるためでは
> ありません。

## 3. 数え直し方

```sh
python3 scripts/handover_timeouts.py            # 表と実物が合っているか
python3 scripts/handover_timeouts.py --print    # 実物から表を出す
python3 scripts/handover_timeouts.py --apply    # 表を実物に合わせて書き直す
```

`--check` は受け側の `workflow-lint.yml` と `scripts/verify.sh` の両方で
走ります。**合計だけでなく 1 行ずつ**見るので、値が 1 つ書き換わっても
落ちます —— 合計は動かないので、そこだけを見ていると素通りします。

道具ごと消えた木で数えるなら、下でも足ります:

```sh
cd .github/workflows
python3 - <<'PY'
import glob, yaml
job = step = 0
for f in sorted(glob.glob('*.yml')):
    for jn, j in (yaml.safe_load(open(f)).get('jobs') or {}).items():
        t = j.get('timeout-minutes')
        print(f"{f}\t{jn}\t{t if t is not None else 'MISSING'}")
        job += t is not None
        for s in (j.get('steps') or []):
            if 'timeout-minutes' in s:
                step += 1
                print(f"    ↳ {s.get('name') or s.get('uses')}\t{s['timeout-minutes']}")
print(f"job={job} step={step} total={job+step}")
PY
```

`MISSING` が 1 つでも出たら、`workflow-lint` と `sync-guard` の両方が
落ちます。

## 4. 受け側にある関連ファイル

写すときの参照用です。**何を守っているかは、コードのコメントが正です。**

| ファイル | 何が書いてあるか |
| --- | --- |
| `.github/workflows/workflow-lint.yml` | 欠落検査の本体（§1-1） |
| `scripts/handover_timeouts.py` | この表を生成し、木へ戻す道具（§1-1・§3） |
| `scripts/handover_timeouts_test.py` | その道具の判定を留めるもの |
| `scripts/after_snapshot.py` | **生成器が 1 回だけ呼ぶ入口。** §1-1 と §1-3 を順番どおりに走らせます |
| `scripts/after_snapshot_test.py` | その順番を留めるもの |
| `scripts/verify.sh` | 同じ判定のローカルの写し（§1-1） |
| `.github/workflows/agent-ebpf.yml` | `mkdir -p downloads` の位置（§1-2） |
| `scripts/recalibrate_ratchets.py` | ラチェットの固定値を下げる道具（§1-3） |
| `scripts/reanchor_mutations.py` | 変異仕様書の pattern を追随させる道具（§1-3） |
| `scripts/sync_guard.py` | 受け側のガード本体。何を守っているかの一覧 |

---

**この文書の表は検査で留めてあります。** `workflow-lint` が
「job N + step N ＝ N 件」の行と、**表の 1 行ずつ**を実物と突き合わせるので、
**実物が動いたのにここが古いままだと CI が落ちます。** 直すのは
`--apply` の 1 回です。

**そして、写さないでください。** 同じ表から `--restore` が入れます（§1-1）——
写す手が無くなれば、写し違いも、写し忘れも無くなります。
