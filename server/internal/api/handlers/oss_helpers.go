package handlers

// このファイルはオープンソース版でのみ使われる補助関数です。
// 商用版では、ここにある関数はモバイル系ハンドラ側で定義されています。
// 公開版にはモバイル関連の機能が含まれないため、残るハンドラが参照する
// 小さな共有ヘルパだけをここに切り出しています。

// orUnknown は空文字列を「不明」に置き換えます。
// 表示用途のみ。値の有無で分岐する処理には使わないこと。
func orUnknown(s string) string {
	if s == "" {
		return "不明"
	}
	return s
}
